import { BrNumberFormatPipe } from './br-number-format.pipe';

describe('BrNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BrNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
